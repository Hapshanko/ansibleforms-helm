apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "skel.fullname" . }}-test-connection"
  labels:
    {{- include "skel.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "skel.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
